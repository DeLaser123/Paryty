apiVersion: v1
kind: Service
metadata:
  name: paryty-query
  namespace: paryty
  labels:
    app.kubernetes.io/name: paryty-query
    app.kubernetes.io/component: query
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: paryty-query
