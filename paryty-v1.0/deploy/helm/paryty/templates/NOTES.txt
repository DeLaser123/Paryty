Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To get the list of pods running in your cluster:

  kubectl get pods -l "app.kubernetes.io/instance={{ .Release.Name }}" -n {{ .Values.global.namespace | default .Release.Namespace }}

To get the URL of the frontend service:

{{- if .Values.frontend.ingress.enabled }}
{{- range $host := .Values.frontend.ingress.hosts }}
  http{{ if $.Values.frontend.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}
{{- else if contains "ClusterIP" .Values.frontend.service.type }}
  kubectl port-forward svc/{{ .Release.Name }}-frontend {{ .Values.frontend.service.http.port }}:{{ .Values.frontend.service.http.port }} -n {{ .Values.global.namespace | default .Release.Namespace }}
  Then visit: http://localhost:{{ .Values.frontend.service.http.port }}
{{- end }}

For more information, visit: https://paryty.io/docs
