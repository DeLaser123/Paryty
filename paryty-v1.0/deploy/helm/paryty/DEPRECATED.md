# DEPRECATED — Superseded file removed

This file incorrectly claimed `cluster/deploy/helm/` as the canonical chart system.
The authoritative Helm chart is `deploy/helm/paryty/` (umbrella chart with subcharts)
per locked architectural decision (Phase 8 — Helm Chart: Umbrella with Subcharts).

`cluster/deploy/helm/` is DEPRECATED. See `cluster/deploy/helm/DEPRECATED.md`.
