apiVersion: v1
kind: Service
metadata:
  name: paryty-pipeline
  labels:
    app.kubernetes.io/component: pipeline
spec:
  ports:
    - port: 8080
      name: grpc
      targetPort: grpc
    - port: 8081
      name: health
      targetPort: health
  selector:
    app.kubernetes.io/component: pipeline
